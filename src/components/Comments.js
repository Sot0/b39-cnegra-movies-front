import React, { Fragment } from 'react';

const Comments = ({comments}) => {
    if(!comments) return <h3>Aún no hay opiniones</h3>
    return (
        <Fragment>

        </Fragment>
    );
}
 
export default Comments;